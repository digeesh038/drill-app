#! mongo-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
    name: mongo
    namespace: minidrill
    labels:
        com.docker.compose.project: minidrill
        com.docker.compose.service: mongo
spec:
    replicas: 1
    selector:
        matchLabels:
            com.docker.compose.project: minidrill
            com.docker.compose.service: mongo
    strategy:
        type: Recreate
    template:
        metadata:
            labels:
                com.docker.compose.project: minidrill
                com.docker.compose.service: mongo
                com.docker.compose.network.mini-network: "true"
        spec:
            restartPolicy: Always
            containers:
                - name: mini-mongo
                  image: mongo:6.0
                  imagePullPolicy: IfNotPresent
                  ports:
                    - name: mongo-27017
                      containerPort: 27017
                  volumeMounts:
                    - name: data-db
                      mountPath: /data/db
            volumes:
                - name: data-db
                  persistentVolumeClaim:
                    claimName: mongo-mongo-data
